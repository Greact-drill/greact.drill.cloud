services:
  cloud-api-dev-common:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - CURRENT_API_BASE_URL
    restart: always
    expose:
      - '3000'
    depends_on:
      cloud-db-dev-common:
        condition: service_healthy
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
      - CURRENT_API_BASE_URL=${CURRENT_API_BASE_URL}
    networks:
      - proxy

  cloud-db-dev-common:
    image: postgres:17
    container_name: cloud-common-database-container
    restart: always
    expose:
      - '5432'
    volumes:
      - cloud-db-dev-common:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    networks:
      - proxy
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  cloud-db-dev-common:

networks:
  proxy:
    external: true  
