services:
  cloud-api-dev:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    expose:
      - '3000'
    depends_on:
      - cloud-database-container
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
    networks:
      - proxy

  cloud-db-dev:
    image: postgres:17
    container_name: cloud-database-container
    restart: always
    expose:
      - '5432'
    volumes:
      - cloud-db-dev:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    networks:
      - proxy      

volumes:
  cloud-db-dev:

networks:
  proxy:
    external: true  
